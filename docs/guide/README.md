# Guide

Add the site documentation here.

## Search

Page headings and content are indexed by the site search.

## Manim diagrams

Add scenes to `docs/manim`, register them in `scripts/render-manim-diagrams.mjs`, then run:

```sh
pnpm docs:manim
```
